'use strict'

angular.module '<%= scriptAppName %>'
.component '<%= cameledName %>', {
    template: '<div></div>'
    controller: () ->
        this.$onInit ->

        this.$onChanges ->

        this.$onDestroy ->
}
