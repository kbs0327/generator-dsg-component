'use strict'

angular.module '<%= scriptAppName %>'
.component '<%= cameledName %>', {
    templateUrl: '<%= htmlUrl %>'
    controller: () ->
        this.$onInit ->

        this.$onChanges ->

        this.$onDestroy ->
}
